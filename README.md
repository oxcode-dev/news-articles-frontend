# React + TypeScript + Vite

Install NPM dependencies:

```sh
yarn install
```

Build assets:

```sh
yarn run dev
```

Setup configuration:

```sh
cp .env.example .env
```

- **Username:** johndoe@example.com
- **Password:** secret